import numpy as np
a = np.array([9, 1, 2]) # Die Hard
b = np.array([8, 3, 8]) # Deadpool
c = np.array([1, 9, 2]) # Notebook
print("Die Hard . Deadpool:", np.dot(a, b))
print("Die Hard . Notebook:", np.dot(a, c))